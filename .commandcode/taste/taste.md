# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# firefox
- For Firefox MV3 extensions, use `background.scripts` instead of `background.service_worker` in manifest.json. Confidence: 0.70
